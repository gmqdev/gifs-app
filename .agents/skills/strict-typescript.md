# Skill: Strict TypeScript

- Strictly avoid using `any`.
- Define strict types using interfaces and generics.
- Store interfaces in their corresponding domain's `interfaces/` directory.
- Typing must be descriptive and accurately reflect API responses and component props.
