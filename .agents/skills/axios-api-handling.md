# Skill: Axios API Handling

- Use the centralized Axios instances located in `src/gifs/api/` to maintain a unified base URL and interceptors.
- Handle network errors gracefully and consistently across all API calls.
